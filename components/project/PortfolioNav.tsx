import { SiteHeader } from "@/components/site/SiteChrome";

/** @deprecated Use SiteHeader. Kept as an alias so old imports stay consistent. */
export function PortfolioNav() {
  return <SiteHeader />;
}
